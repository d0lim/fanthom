[project]
name = "fanthom-ai-pipeline"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "yt-dlp>=2024.0.0",
    "demucs>=4.0.0",
    "basic-pitch>=0.3.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]
